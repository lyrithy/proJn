<% include ../partials/header %>
<%  if(currentUser!=null&&currentUser.isAdmin){ %>  
    <a href="/adminpanel">Go to admin panel</a>
    <%  } else{ %>
   
<div class="body">
<div class="container">
    <div class="signup">
            <form action="/Services/<%= Service._id %>/comments/" method="POST">
                <h2>Add New Comment to <%= Service.name %></h2>
                <div class="form">
                    <input class="input" type="text" name="comment[text]" placeholder="text" required>
                </div>
                <div class="form">
                    <button class="submit">Submit!</button>
                </div>
                </form>
                <a href="/Services/<%= Service._id %>">Go Back</a>
        </div>
    </div>
</div>
<% } %>
<% include ../partials/footer %>

